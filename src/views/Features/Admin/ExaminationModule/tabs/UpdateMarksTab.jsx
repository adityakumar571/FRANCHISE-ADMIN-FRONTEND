/* eslint-disable prettier/prettier */
/**
 * Tab 2 — Update Marks (manual entry per student)
 * Shows class-wise marks summary with Add/Edit marks modal.
 * Re-uses the existing Marks page.
 */
import React from 'react'
import Marks from '../../../../pages/Marks/Marks'

const UpdateMarksTab = () => <Marks />

export default UpdateMarksTab
